apply plugin: 'com.android.application'

android {
    namespace 'com.example.webrtc'  // 改成你的包名
    compileSdk 35

    defaultConfig {
        applicationId "com.example.webrtc"
        minSdk 21
        targetSdk 35
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    // 修复1：使用真实存在的 WebRTC 版本（固定）
    implementation 'org.webrtc:google-webrtc:1.0.32406'

    // 修复2：使用真实存在的 PubNub 版本
    implementation 'com.pubnub:pubnub-gson:4.38.0'

    // 你的原始依赖（保留）
    implementation 'io.pristine:libjingle:9694@aar'
    implementation 'me.kevingleason:pnwebrtc:1.0.6@aar'

    // 基础库
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'com.google.android.material:material:1.12.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'

    // 测试
    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.2.1'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.6.1'
}
