buildscript {
    ext {
        agp_version = '8.2.2'
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:$agp_version"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()  // 最新依赖在这里
        maven { url 'https://jitpack.io' }  // 如需老依赖
    }
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
